<?php
 use Drupal\Core\Routing\RouteMatchInterface;

function reserve_movies_theme($existing, $type, $theme, $path){

    return array(
        'movie-list' => array(
            'variables' => array('movies' => array(), 'title' => '', 'genres' => array())
        )
    );
}
